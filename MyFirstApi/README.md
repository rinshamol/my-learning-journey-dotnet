## 📌 Project: Serialization & Deserialization (Console App)

This project demonstrates a controller-based Web API in ASP.NET Core with basic CRUD operations for a list of products.

## Project Overview

- Controller: ProductsController
- Implements basic CRUD operations for products using HTTP methods:
    GET → Retrieve products
    POST → Add a new product
    PUT → Update an existing product
    DELETE → Delete a product

- Returns simple string responses to demonstrate each operation.

## 🚀 How to Run

1. Clone the repository:
`git clone https://github.com/rinshamol/my-learning-journey-dotnet.git`
2. Navigate into the project:
`cd my-learning-journey-dotnet/MyFirstApi/`
4. Restore dependencies:
`dotnet restore`
5. Run the app:
`dotnet run`

## 🛠 Technologies Used

- **C#**
- **.NET SDK 10**
- **ASP.NET Core Web API**
- **HTTP Client / Postman**
